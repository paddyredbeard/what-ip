service: what-ip
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-west-2

functions:
  json:
    handler: handler.index
    events:
      - httpApi:
          path: /
          method: get
  plaintext:
    handler: handler.plain
    events:
      - httpApi:
          path: /plain
          method: get

plugins:
  - serverless-offline
